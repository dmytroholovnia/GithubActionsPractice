name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello world!"
      - name: Say bye
        run: echo "Bye!"
        
  
